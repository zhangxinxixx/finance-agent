# 数据库配置
# 本地开发默认使用用户态 PostgreSQL，避免与系统 PostgreSQL 5432 冲突。
# 可通过 DATABASE_URL 环境变量覆盖。

default:
  host: 127.0.0.1
  port: 55432
  database: finance_agent
  user: finance_agent
  password: "${DATABASE_PASSWORD}"
  pool_size: 5
  echo_sql: false
